# `npm run dev` and `npm start` load this before Next.js starts.
PORT=3010

# Browser-visible base URL of the local CVFuzz API (`cvfuzz serve`).
# Docker Compose builds this as `/api` so the browser stays on the frontend origin.
NEXT_PUBLIC_CVFUZZ_API_URL=http://127.0.0.1:8020
